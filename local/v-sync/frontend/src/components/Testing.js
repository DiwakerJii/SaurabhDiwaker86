// import React from 'react'
// // import Logout from './Logout'
// import HealthQuestions from './HealthQuestions'
// import SelfieInstant from './dashboard/SelfieInstant'
// import Loader from "./Loader"
// import VoiceBar from "./dashboard/VoiceBar"
// import Recorder from "./dashboard/Recorder"
// import PaymentRedirection from "./dashboard/PaymentRedirection"
import React, { useEffect, useRef, useState } from 'react';
import html2canvas from 'html2canvas';



const Testing = () => {
    // const containerRef = useRef(null);
    // const [src, setSrc] = useState()
    // useEffect(() => {
    //     setSrc(`data:image/png;base64,${string}`)
    // }, [src])

    // const handleCaptureClick = () => {
    //     const container = containerRef.current;

    //     html2canvas(container).then((canvas) => {
    //         const screenshot = canvas.toDataURL('image/png');
    //         const downloadLink = document.createElement('a');
    //         downloadLink.href = screenshot;
    //         downloadLink.download = 'screenshot.png';
    //         downloadLink.click();
    //     });
    // };

    return (

        <div>

        </div>

    );
}


export default Testing


// // in both the components make transparent bg so that peehce wala sb becomes disable

